import from "react";

describe('<Transaction />', () => {
    it('Should render tabs with 3 steps for transaction', () => {

    });
    it('Should have a "Next" button', () => {});
    // it('Should render a Conversion component', () => {});
    it('Should receive quantity to trasnfer', () => {});
    // it('Should render a Footer component', () => {});
    // it('Should transform a quantity to other currency', () => {});
})